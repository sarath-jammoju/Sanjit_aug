name: Deploy to Hostinger

on:
  push:
    branches:
      - main   # or master (use your repo’s default branch)

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'  # use your Node version

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.147.93.99.192 }}
          username: ${{ secrets.u347088786 }}
          password: ${{ secrets.Sanjit@1212024 }}
          local-dir: dist
          # server-dir: /public_html   # change if your site is in a subfolder
          server-dir: /dist   # change if your site is in a subfolder
